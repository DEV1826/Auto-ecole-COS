/**
 * @module config
 * @description Point d'entrée unique pour toute la configuration de l'application
 * @author Stive Junior
 * @version 1.0.0
 *
 * Ce module regroupe et exporte toutes les configurations pour un import simplifié.
 *
 * @example
 * ```typescript
 * import { appConfig, ROUTES, API_PATHS } from '@/config'
 * ```
 */

export * from './app.config';
export * from './routes';
